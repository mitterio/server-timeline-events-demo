import { AccessKey } from './AccessKey';
import { AccessSecret } from './AccessSecret';
export declare class AccessCredential {
    accessKey: AccessKey;
    accessSecret: AccessSecret;
    createdAt: number;
    constructor(accessKey: AccessKey, accessSecret: AccessSecret, createdAt: number);
}
