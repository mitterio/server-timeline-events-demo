import { AccessKey } from './AccessKey';
export declare class AccessCredentialStub {
    accessKey: AccessKey;
    lastUsed: number;
    createdAt: number;
    lastAction: string;
    constructor(accessKey: AccessKey, lastUsed: number, createdAt: number, lastAction: string);
}
