import IdentifiableEntity from '../annotations/IdentifiableEntity';
import { UserLocator } from './locators/UserLocator';
export declare class User implements IdentifiableEntity<User> {
    userId: string;
    userLocators: Array<UserLocator>;
    screenName: {
        screenName: string;
    };
    systemUser: boolean;
    synthetic: boolean;
    constructor(userId: string, userLocators: Array<UserLocator>, screenName: {
        screenName: string;
    }, systemUser?: boolean, synthetic?: boolean);
    identifier(): string;
}
