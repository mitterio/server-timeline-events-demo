export declare class ContextFreeMessage {
    contextType: string;
    context: string;
    senderId: string;
    data: object;
    constructor(contextType: string, context: string, senderId: string, data: object);
}
