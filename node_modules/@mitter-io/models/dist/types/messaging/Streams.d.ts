import IdentifiableEntity from '../annotations/IdentifiableEntity';
export declare class Stream implements IdentifiableEntity<Stream> {
    streamId: string;
    type: string;
    supportedContentTypes: Array<string>;
    constructor(streamId: string, type: string, supportedContentTypes: Array<string>);
    identifier(): string;
}
export declare class ChannelStream {
    channelId: string;
    stream: ChannelStream;
    constructor(channelId: string, stream: ChannelStream);
}
