import IdentifiableEntity from '../annotations/IdentifiableEntity';
export declare class Application implements IdentifiableEntity<Application> {
    applicationId: string;
    name: string;
    sandboxed: boolean;
    constructor(applicationId: string, name: string, sandboxed: boolean);
    identifier(): string;
}
