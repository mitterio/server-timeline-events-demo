import { MitterAccessCredentials } from './credentials-base';
export interface UserCredentials extends MitterAccessCredentials {
    readonly userId: string;
}
export declare class UserAuthorizationCredentials implements UserCredentials {
    readonly userAuthorization: string;
    readonly userId: string;
    constructor(userAuthorization: string, userId: string);
}
