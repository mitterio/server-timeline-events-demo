import { Mitter } from '../Mitter';
import { User, UserLocator } from '@mitter-io/models';
import { MitterObject } from './mitter-objects';
export default class MitterUser extends MitterObject<MitterUser, User> {
    private readonly mitter;
    readonly userId: Promise<string>;
    readonly systemUser: Promise<boolean>;
    readonly synthetic: Promise<boolean>;
    readonly screenName: Promise<{
        screenName: string;
    }>;
    readonly identifier: Promise<() => string>;
    readonly userLocators: Promise<UserLocator[]>;
    private readonly _userId;
    private readonly usersClient;
    private readonly channelsClient;
    constructor(mitter: Mitter, userId?: string | undefined);
    channels(): void;
}
