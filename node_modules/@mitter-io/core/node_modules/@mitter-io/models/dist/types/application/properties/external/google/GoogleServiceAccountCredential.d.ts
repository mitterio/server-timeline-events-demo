import GoogleCredential from './GoogleCredential';
export declare class GoogleServiceAccountCredential extends GoogleCredential {
    config: object;
    constructor(config: object, systemName: string, instanceName: string);
}
