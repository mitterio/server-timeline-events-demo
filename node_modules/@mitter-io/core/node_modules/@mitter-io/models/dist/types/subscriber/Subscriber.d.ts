import IdentifiableEntity from '../annotations/IdentifiableEntity';
export declare class Subscriber implements IdentifiableEntity<Subscriber> {
    subscriberId: string;
    userName: string;
    firstName: string;
    lastName: string;
    email: string;
    mobile: string;
    constructor(subscriberId: string, userName: string, firstName: string, lastName: string, email: string, mobile: string);
    identifier(): string;
}
