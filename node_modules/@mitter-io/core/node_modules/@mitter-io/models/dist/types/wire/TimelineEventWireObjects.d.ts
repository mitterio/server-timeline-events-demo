import { TimelineEvent } from '../messaging/TimelineEvents';
import { ChannelParticipation, ParticipationStatus } from '../messaging/Channels';
export declare class MessageTimelineEvent {
    messageId: string;
    timelineEvent: TimelineEvent;
    channelId: string | null;
    constructor(messageId: string, timelineEvent: TimelineEvent, channelId: string | null);
}
export declare class MessageTimelineEventSet {
    messageId: string;
    timelineEvents: Array<TimelineEvent>;
    channelId: string | null;
    constructor(messageId: string, timelineEvents: Array<TimelineEvent>, channelId: string | null);
}
export declare class ChannelTimelineEvent {
    channelId: string;
    timelineEvent: TimelineEvent;
    constructor(channelId: string, timelineEvent: TimelineEvent);
}
export declare class ChannelTimelineEventSet {
    channelId: string;
    timelineEvents: Array<TimelineEvent>;
    constructor(channelId: string, timelineEvents: Array<TimelineEvent>);
}
export declare class PatchChannelParticipation {
    newStatus: ParticipationStatus;
    constructor(newStatus: ParticipationStatus);
}
export declare class PatchChannelParticipationResult {
    oldChannelParticipation: ChannelParticipation;
    newChannelParticipation: ChannelParticipation;
    constructor(oldChannelParticipation: ChannelParticipation, newChannelParticipation: ChannelParticipation);
}
