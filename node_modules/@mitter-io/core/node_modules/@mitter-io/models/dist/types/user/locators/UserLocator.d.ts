import IdentifiableEntity from '../../annotations/IdentifiableEntity';
import { VerificationStatus } from '../VerificationStatus';
export declare abstract class UserLocator implements IdentifiableEntity<UserLocator> {
    locatorSerializationPrefix: string;
    userLocatorId: string;
    verificationStatus: VerificationStatus;
    protected constructor(locatorSerializationPrefix: string, userLocatorId: string);
    locator(): string;
    identifier(): string;
    protected abstract serializedLocator(): string;
}
