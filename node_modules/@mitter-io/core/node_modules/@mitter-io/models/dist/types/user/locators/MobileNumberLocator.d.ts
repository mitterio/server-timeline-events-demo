import { UserLocator } from './UserLocator';
export declare class MobileNumberLocator extends UserLocator {
    phoneNumber: string;
    static LOCATOR_SERIALIZED_PREFIX: string;
    constructor(phoneNumber: string, userLocatorId: string);
    protected serializedLocator(): string;
}
