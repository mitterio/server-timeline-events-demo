import GoogleCredential from './GoogleCredential';
export declare class GoogleOAuthCredential extends GoogleCredential {
    clientId: string;
    clientSecret: string;
    constructor(clientId: string, clientSecret: string, systemName: string, instanceName: string);
}
