import { UserLocator } from './UserLocator';
export declare class EmailUserLocator extends UserLocator {
    email: string;
    static LOCATOR_SERIALIZED_PREFIX: string;
    constructor(email: string, userLocatorId: string);
    protected serializedLocator(): string;
}
