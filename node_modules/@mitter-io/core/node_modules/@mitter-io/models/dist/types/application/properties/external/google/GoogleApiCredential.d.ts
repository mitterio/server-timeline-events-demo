import GoogleCredential from './GoogleCredential';
export declare class GoogleApiCredential extends GoogleCredential {
    apiKey: string;
    constructor(apiKey: string, systemName: string, instanceName: string);
}
