export declare class MetadataEntry {
    payload: string;
    systemEntry: boolean;
    constructor(payload: string, systemEntry: boolean);
}
declare type metadataType = {
    [propname: string]: MetadataEntry;
};
export declare class EntityMetadata {
    metadata: metadataType[];
    constructor(metadata: metadataType[]);
}
export interface MetadataAttachable {
    entityMetaData: EntityMetadata;
}
export {};
