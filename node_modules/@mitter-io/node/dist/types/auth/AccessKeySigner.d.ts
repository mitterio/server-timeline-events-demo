import { DigestParts, DigestGenerationArtifacts } from './digest-objects';
export default class AccessKeySigner {
    readonly accessKey: string;
    private readonly accessSecret;
    constructor(accessKey: string, accessSecret: string);
    static Headers: {
        Nonce: string;
        Date: string;
        ContentMD5: string;
        ContentType: string;
    };
    signRequest(digestParts: DigestParts): DigestGenerationArtifacts;
    private getNonce;
    private getContentType;
    private getDate;
}
